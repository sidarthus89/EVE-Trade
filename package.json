{
    "name": "eve-data-site",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "git add . && git commit && git push origin main",
        "fetch:regions": "node scripts/fetchRegions.js",
        "fetch:stations": "node scripts/fetchStations.js",
        "fetch:all": "npm run fetch:regions && npm run fetch:stations"
    },
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "react-router-dom": "^6.20.0"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.2.0",
        "concurrently": "^9.2.1",
        "vite": "^5.0.0"
    }
}